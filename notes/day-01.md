# Handmade Hero – Day 1

- Set up compiler
- Set up command prompt on Windows
    - Command Prompt Properties -> add `/k w:\hmh\handmade\misc\shell.bat` to be able to access the compiler in the terminal
- Set up text editor
    - Neovim with Lazy Vim
        [] Setup build shortcut
- Set up build.bat
- Created the main entry point using `WinMain`.  
- Displayed a simple **MessageBox** as a test to ensure the application runs.  
